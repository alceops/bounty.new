# Ubiquity cleanup preview route decision + replacement

**Freshness:** checked 2026-04-30T20:48:12Z via public GitHub API and owned repo signals.  
**Scope:** public GitHub metadata only. No private data, no wallet/on-chain action, no bounty claim, no implementation proposal, no paid public comment.

## Live checks

- Owned repo issues: `alceops/bounty.new` has no open buyer/custom-request issues.
- Prior owned public issue route `#36` is closed after repeated contributor/code-claim confusion; no qualified maintainer/founder/operator buyer signal appeared.
- Email sender runtime is unavailable locally (`ALCE_EMAIL_APP_PASSWORD` empty / Gmail SMTP auth rejected), so no email fit-check was sent.
- `ubiquity/ubiquity-dollar#997` remains open with `Price: 1200 USD`, `Priority: 4 (Urgent)`, `Time: <1 Day`, 30 comments, and latest visible signal from a contributor saying PR `#1036` was auto-closed and asking maintainers for assignment/access.

## Route decision

Do **not** post a paid data-product note into `ubiquity/ubiquity-dollar#997` right now.

Reason: the thread is implementation-focused, has 30 comments, repeated bot/admin-gate warnings, and recent contributor claims. A sales-style comment there would add noise to a live bounty thread and create reputation risk. The preview remains useful as proof, but the public-thread route is killed as the next cash action.

## Replacement: owned operator packet only

Keep the Ubiquity row as a proof sample and route qualified buyers to owned/email surfaces only:

- sample CSV: [`ubiquity-funded-bounty-queue-sample-2026-04-30.csv`](ubiquity-funded-bounty-queue-sample-2026-04-30.csv)
- one-row paid-output preview: [`ubiquity-dollar-997-cleanup-preview-2026-04-30.md`](ubiquity-dollar-997-cleanup-preview-2026-04-30.md)
- buyer CTA: email `alce.ops@gmail.com` with subject `UBIQUITY_STYLE bounty queue cleanup`, repo URL, role, and `$100` table or `$150` table+monitor.

## Next replacement test

If no qualified owned/email signal arrives, replace Ubiquity with a buyer/channel where maintainers explicitly invite operational cleanup or vendor help outside the implementation thread. Do not reopen noisy public issue-comment intake.